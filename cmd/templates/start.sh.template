#!/usr/bin/env bash

# 用法: bash ./build/scripts/start_{{ .Name }}.sh

set -e

# 进入项目根目录（脚本在 build/scripts 下）
cd "$(dirname "$0")/../.." || exit 1

echo "🚀 正在从项目根目录执行 go run {{.MainPath}} ... 进行项目启动"

go run "{{.MainPath}}" \
  --config="{{.ConfigPath}}" \
  --env="{{.EnvPath}}" \
  --show=true